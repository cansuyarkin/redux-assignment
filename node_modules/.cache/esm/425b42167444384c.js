let createStore;_744‍.x([["addTask",()=>addTask],["removeTask",()=>removeTask],["default",()=>_744‍.o]]);_744‍.w("redux",[["createStore",["createStore"],function(v){createStore=v}]]);

// ACTION TYPES

const ADD_TASK = "ADD_TASK";
const REMOVE_TASK = "REMOVE_TASK";

// ACTION CREATORS

       const addTask = (id, title) => ({
    type: ADD_TASK,
    payload: { id, title } //{id: id, title: title}
})

       const removeTask = (id) => ({
    type: REMOVE_TASK,
    payload: id //{id: id, title: title}
})

// REDUCER

const tasksReducer = (tasks = [], action) => {
    switch (action.type) {
        case ADD_TASK:
            return [...tasks, action.payload];
        case REMOVE_TASK:
            return tasks.filter((item) => item.id !== action.payload)
        default:
            return tasks;
    };
};

_744‍.d(tasksReducer);